# data & heavy artifacts — never commit (see INTEGRITY.md)
# NB: anchored with a leading slash so these only match the repo-root dirs and do NOT
# swallow the src/carve/data package.
/data/
/weights/
/checkpoints/
*.pt
*.pth
*.ckpt
*.safetensors

# experiment outputs (keep experiments/ dir + README + curated tables only)
experiments/runs/
experiments/**/runs/
# derived example images are NOT committed: they are pixel derivatives of ISIC-2018 /
# HAM10000 (CC BY-NC-SA 4.0) real patient photos — regenerate locally, don't redistribute.
experiments/artifact_examples/
*.parquet
wandb/

# python
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# os / editor
.DS_Store
.idea/
.vscode/

# local-only convenience scripts
docker-run.sh
